{
  "name": "esbuild",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.mjs",
    "watch": "./node_modules/.bin/esbuild --loader:.js=jsx src/app.js --bundle --outfile=dist/out.js --watch",
    "serve": "./node_modules/.bin/esbuild --loader:.js=jsx src/app.js --bundle --outfile=dist/out.js --serve"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.19.4"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
